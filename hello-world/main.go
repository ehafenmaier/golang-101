// The 'main' package declaration is only used for building an executable file
// and must also contain a 'main()' function. Any other package declaration will
// build a reusable library file.
package main

import "fmt"

func main() {
	fmt.Println("Hi there!")
}
